---
title: "Yixiutong execution log"
link: "yixiutong-execute-log"
type: debug_history
tags: [execute, yixiutong]
uuid: "execute-2026-04-03-yixiutong"
created_at: "2026-04-03T00:28:20+08:00"
plan_path: ".artifacts/plan/2026-04-03_00-10-06_翼修通_实现计划.md"
start_commit: "N/A"
env: {target: "local", notes: "Non-git workspace; all project artifacts must remain under current D-drive workspace."}
---

## Pre-Flight Checks

- Branch: N/A
- Rollback commit: N/A, current workspace is not a git repository
- D drive free space at start: 26.64 GB
- Project directory size at start: 2.9 MB
- Python: 3.11.4
- npm: 10.2.3
- DoR satisfied: yes
- Access/secrets: not required for scaffold; cloud LLM credentials still pending for live inference
- Fixtures/data: created under `materials/`

## Execution Notes

### 2026-04-03 00:28

- Created controlled workspace directories: `materials/`, `models/`, `runtime/`, `yixiutong-mvp/`
- Started first-wave implementation:
  - path-constrained config
  - backend scaffold
  - dual-provider abstraction
  - demo knowledge pack
  - minimal diagnosis workflow

### 2026-04-03 00:45

- Created backend scaffold under `yixiutong-mvp/apps/api`
- Added unified storage config with controlled roots for:
  - project
  - materials
  - models
  - runtime
  - caches
  - index
  - logs
  - db
- Added dual-provider abstraction:
  - `openai_compatible`
  - `ollama`
- Added demo knowledge packs under `materials/`
- Added minimal diagnosis workflow with:
  - route
  - retrieve
  - diagnose
  - feedback
- Added single-page React demo under `yixiutong-mvp/apps/web`

### 2026-04-03 00:52

- Created local virtual environment at `yixiutong-mvp/.venv`
- Installed Python dependencies using D-drive pip cache only
- Installed web dependencies using D-drive npm cache only
- Built retrieval index successfully:
  - `runtime/index/index.json`
- Built front-end successfully:
  - `yixiutong-mvp/apps/web/dist`
- Executed tests:
  - `5 passed`
- Verified runtime self-check endpoint:
  - status `200`
- Verified diagnosis -> confirm -> feedback full loop:
  - all endpoints returned `200`
- Feedback database created under:
  - `runtime/db/feedback.sqlite3`

### 2026-04-03 09:38

- Extended the backend to cover three scene types:
  - `fault_diagnosis`
  - `process_deviation`
  - `quality_inspection`
- Added scene-aware routing, retrieval boosts, and risk rules
- Added provider runtime helpers for:
  - primary/fallback provider checks
  - structured generation with fallback
  - local model discovery
- Added a local 3B fallback download flow:
  - `scripts/download_local_model.py`
  - `scripts/download_local_model.ps1`
- Added an Ollama registration helper:
  - `scripts/register_ollama_model.ps1`
- Locked the local model budget to `3 GB` in config and `.env.example`
- Verified the fallback model source repository:
  - `Qwen/Qwen2.5-3B-Instruct-GGUF`
- Expanded demo materials with process and quality scene knowledge packs plus setup templates
- Rewrote the front-end text layer in ASCII to remove mojibake and avoid future encoding regressions
- Added full review-loop UI wiring for:
  - diagnosis start
  - human approval or rejection
  - feedback submission
- Rewrote `README.md` with:
  - D-drive storage constraints
  - cloud provider setup
  - local fallback setup
  - guardrails and verification commands
- Rewrote API tests with ASCII fixtures and added explicit confirm/feedback coverage
- Re-ran verification:
  - backend tests: `10 passed`
  - front-end build: success
- Runtime self-check after continuation:
  - free space: `30.29 GB`
  - project size reported by API: `206.53 MB`
  - provider: `openai_compatible`
  - fallback provider: `ollama`
  - local model enabled: `false`
  - local model present: `false`
- Provider check after continuation:
  - primary: `not configured`
  - fallback: `local fallback disabled`
- Diagnosis sanity check after continuation:
  - scene: `fault_diagnosis`
  - provider used: `heuristic_fallback`
  - risk level: `high`
  - evidence count: `5`

### 2026-04-03 12:46

- Confirmed explicit local Ollama executable path:
  - `D:/develop_tool/ollama/Ollama/ollama.exe`
- Validated the provided cloud credential against the official OpenAI-compatible endpoint:
  - target: `https://api.openai.com/v1`
  - masked key preview: `sk-e75...c694`
  - result: `401 invalid_api_key`
  - report saved to: `runtime/logs/primary_provider_validation.json`
- Tightened primary provider readiness rules:
  - `openai_compatible` now requires `base_url`, `model`, and `api_key`
- Added text-generation fallback support in provider runtime
- Added diagnosis text-assist parsing and unit coverage
- Updated API tests to run in a fast local mode without blocking on local inference
- Fixed the local Ollama registration flow by adding the Qwen chat template and stop tokens to the generated Modelfile
- Re-registered the local model:
  - `yixiutong-qwen3b`
- Re-verified the local fallback path:
  - `scripts/start_ollama.ps1` reports Ollama reachable
  - fallback provider check: reachable
- Re-ran verification:
  - backend tests: `17 passed`
  - front-end build: success
- Real diagnosis check after Ollama template fix:
  - request: `E-204 + vibration abnormal + temperature rise`
  - response status: `200`
  - provider used: `ollama:fallback:yixiutong-qwen3b`
  - diagnosis returned structured causes, checks, and actions
- Runtime self-check after completion:
  - free space: `21.54 GB`
  - project size reported by API: `4221.44 MB`
  - local model enabled: `true`
  - local model present: `true`
  - ollama executable present: `true`

### 2026-04-03 12:56

- Added one-click launcher scripts:
  - `scripts/start_yixiutong.ps1`
  - `scripts/start_yixiutong.cmd`
  - `scripts/run_api.ps1`
  - `scripts/run_web.ps1`
- Launcher behavior:
  - bootstraps D-drive cache paths
  - starts Ollama if needed
  - opens dedicated backend and frontend PowerShell windows
  - waits for `http://127.0.0.1:8000/api/v1/system/self-check`
  - waits for `http://127.0.0.1:5173`
  - writes logs to `runtime/logs/launcher`
- Fixed launcher issues caused by:
  - inherited PowerShell profile loading conda hooks
  - stderr log lines from `uvicorn` and `vite` being treated as terminating errors
- Verified one-click startup end-to-end:
  - backend self-check returned `200`
  - frontend root returned `200`

### 2026-04-03 13:09

- Reworked the front-end from a single demo page into an OA-style portal shell
- Added portal modules:
  - `#/dashboard`
  - `#/fault`
  - `#/process`
  - `#/quality`
  - `#/knowledge`
  - `#/ops`
- Extracted shared front-end state into:
  - `apps/web/src/hooks/useYixiutongWorkspace.ts`
- Added new portal pages:
  - `DashboardPage`
  - `KnowledgeCenterPage`
  - `SystemOpsPage`
- Refactored the diagnosis page into a reusable business seat under the OA shell
- Expanded the self-check type surface on the front-end to show:
  - primary/fallback base URLs
  - Ollama executable presence
- Rebuilt the front-end successfully after the portal refactor:
  - `npm run build`
- Re-verified one-click startup after the portal refactor:
  - backend self-check returned `200`
  - frontend root returned `200`

## Current Footprint

- `yixiutong-mvp`: approx. 135 MB
- `runtime`: approx. 69 MB
- `materials`: under 1 MB
- `models`: approx. 4.0 GB
- D drive free space after current continuation: 20.90 GB

## Notes

- The local 3B fallback model is now downloaded, registered, and usable through Ollama
- The primary cloud path remains disabled because the validated key returned `401 invalid_api_key` on `2026-04-03`
- Current diagnosis runs can use `ollama:fallback:yixiutong-qwen3b` when the primary channel is unavailable
- The explicit Ollama executable path removes the dependency on the shell PATH
- All intentionally created project outputs remain under the current D-drive workspace
- The PowerShell environment emits a noisy conda activation parser error on every command; it does not block execution and no project artifacts depend on it

### 2026-04-03 16:24

- Continued the OA completion pass under the repository-local `superpowers` workflow
- Extended the back-end verification surface with new API tests:
  - auth login and current-user profile
  - portal overview and role-based work-order visibility
  - diagnosis to work-order creation linkage
  - knowledge library list/detail
  - notification channel permission, update, and test flows
- Added a new regression file:
  - `tests/api/test_portal_oa_api.py`
- Fixed a front-end typing break in the dashboard quick-entry navigation
- Fixed a portal interaction gap:
  - switching business scene from the input form now reloads the matching Chinese preset data
- Fixed a knowledge-center refresh gap:
  - filtering now updates the selected article correctly
  - empty result sets now clear the right-hand document panel
- Tightened logout cleanup:
  - clears selected work order, knowledge article, and last diagnosis result
- Rewrote the project README into a Chinese operator-facing guide:
  - portal modules
  - demo accounts
  - one-click startup
  - cloud/local provider setup
  - major API surface
- Re-ran verification after the OA completion pass:
  - backend tests: `20 passed`
  - front-end build: success

## Latest State

- The web portal now includes:
  - role login shell
  - dashboard
  - business seats for fault / process / quality
  - approvals inbox
  - work-order center
  - online knowledge library
  - notification integration page
  - system status page
- The UI no longer exposes the disk-space guard as a visible operator-facing concern
- System-facing status still retains:
  - primary provider readiness
  - fallback provider readiness
  - local Ollama/model readiness

### 2026-04-03 17:18

- Ran an additional OA self-check focused on:
  - work-order stage visibility
  - approval todo semantics
  - knowledge-center depth
  - panel-level scrolling and reading experience
- Back-end portal model improvements:
  - added work-order `status_bucket` and `status_bucket_label`
  - added approval `status_label`
  - dashboard summary now includes:
    - `pending_execution_count`
    - `rework_count`
  - `/api/v1/portal/approvals` now defaults to pending-only todos
  - `/api/v1/portal/approvals?include_history=true` exposes approval history
  - `/api/v1/portal/work-orders?status_bucket=...` supports stage filtering
- Workflow behavior improvements:
  - approved work orders move to `pending_execution`
  - feedback without final resolution moves eligible orders to `in_progress`
  - final resolution closes orders as `completed`
  - rejected approvals keep orders in `rework`
- Front-end OA refinements:
  - rewrote dashboard to surface richer status cards
  - rewrote approvals page so only true pending items remain in the inbox
  - rewrote work-order center with:
    - status tabs
    - richer detail view
    - evidence summary
    - approval history
  - rewrote knowledge center with:
    - dynamic categories
    - stronger reading layout
    - panel-level independent scrolling
  - markdown renderer now supports links and inline code
- Knowledge base expansion:
  - downloaded official FAA references into `materials/knowledge/official_refs/raw`
  - added Chinese digest documents under `materials/knowledge/official_refs`
  - knowledge service now prefers Markdown H1 titles over raw filenames
- Added a dedicated self-check report:
  - `.artifacts/execute/2026-04-03_oa_self_check_report.md`
- Verification after this continuation:
  - backend tests: `20 passed`
  - front-end build: success
